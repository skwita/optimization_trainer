from core.task_base import TaskBase
from core.subtask import SubTask, Question


class Task6(TaskBase):

    name = "ОТ-06"

    def load_variant(self, variant):

        sub1 = SubTask(
            title="Задача.",
            condition_elements=[
                {"type": "text", "value": """Коммивояжёр должен посетить все города и вернуться в исходный город, пройдя 
минимальное суммарное расстояние. 

Города: A, B, C, D, E, F (N = 6). Начальный и конечный город – A (маршрут 
замкнутый: начинается и заканчивается в A). 

Матрица расстояний dᵢⱼ (строки – откуда, столбцы – куда). Значение ∞ означает, что 
прямой переход запрещён (нельзя идти по этому ребру)."""},

                {"type": "table", "value": {
                    "columns": ["От → Куда", "A", "B", "C", "D", "E", "F"],
                    "data": [
                        ["A", "-", 2, 9, 10, 7, 5],
                        ["B", 3, "-", 6, "∞", 3, 4],
                        ["C", 9, 6, "-", 8, 5, 6],
                        ["D", 11, 4, 8, "-", 6, 3],
                        ["E", 7, 3, 5, 6, "-", 9],
                        ["F", 5, 4, 6, 3, 9, "-"],
                    ]
                }},

                {"type": "text", "value": """Обратите внимание:
• Матрица частично асимметрична (например, dAD = 10, но dDA = 11).
• Прямой переход B → D запрещён (вместо расстояния ставим ∞). Обход возможен 
через другие города.
• Все остальные расстояния – неотрицательные целые числа; диагональные элементы 
(сам в себя) игнорируются.

Ограничение предшествования: Город D должен быть посещён до города E на маршруте 
(т.е. в циклической последовательности D должно идти раньше E при обходе от A и 
возвращении в A).

Цель: Найти замкнутый порядок посещения всех городов (перестановку городов, 
начинающуюся и кончающуюся в A), который минимизирует суммарное расстояние пути:

min ∑ dᵢⱼ по рёбрам (i → j)

при выполнении ограничений:
• каждый город посещён ровно один раз (кроме A, которая – и старт, и финиш),
• переходы по запрещённым рёбрам не используются."""},

                {"type": "text", "value": """Дополнительные технические замечания (рекомендации для реализации SA):

• начальная температура T₀ подбирается так, чтобы вероятность принятия 
ухудшающего шага была заметна на старте;

• схема охлаждения: экспоненциальное Tₖ₊₁ = αTₖ с α ∈ (0.99, 0.9999) 
или логарифмическое;

• соседства: комбинируйте 2-opt и swap (перестановка двух городов) для лучшего 
исследования пространства;

• учитывайте запрет перехода B → D: при 2-opt проверяйте, не создаётся ли это ребро;
если создаётся – либо отбрасывайте шаг, либо назначайте высокую цену;

• для обеспечения предшествования D before E проще работать с представлением 
маршрута как последовательностью (позиции), и при генерации поддерживать 
условие D_index < E_index."""}
            ],
            questions=[
                Question("Оптимальный маршрут (без начального и конечного A, через ;)", "F;D;C;E;B"),
                Question("Минимальная длина маршрута", 27),
            ]
        )

        return [sub1]